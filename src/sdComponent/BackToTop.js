import {h} from 'preact';

const BackToTop = () => {
  return (<div id="back-top" >
          <a href="#top" className="hashAdded">
            <i className="back-to-top-icon backTopImg"></i>
          </a>
        </div>);
}

export default BackToTop;
